"""
Strategy templates for the 150K variant engine.

Each strategy is a TEMPLATE — it evaluates whether its pattern/setup
is present, and outputs a CandidateSignal (not a trade).

Filters are applied separately by the filter engine.
"""
